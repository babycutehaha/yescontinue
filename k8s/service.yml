apiVersion: v1
kind: Service
metadata:
  name: yeslay
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yeslay"
    port: 8080
  selector:
    app: yeslay
